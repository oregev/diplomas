import { StyleSheet } from "@react-pdf/renderer";

export const styles = StyleSheet.create({
  container: {
    textAlign: "center",
    paddingHorizontal: 50,
  },
  pre: {
    fontSize: 18,
    marginBottom: 20,
  },
  title: {
    fontSize: 55,
    marginBottom: 16,
  },
  name: {
    fontSize: 40,
    marginBottom: 16,
  },
  id: {
    fontSize: 16,
    marginBottom: 22,
  },
});
